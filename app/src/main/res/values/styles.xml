<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/myactionOverColor</item>
        <item name="colorPrimaryDark">@color/myactionBackground</item>
        <item name="colorAccent">@color/myactionOverColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="MyActionBarTabBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@drawable/button_default</item>
    </style>

    <style name="AppTheme.ListView">
        <item name="android:background">@color/myListView</item>
    </style>
</resources>
